{
  "name": "dslq-cloudflare",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply dslq_research --local",
    "d1:migrate:remote": "wrangler d1 migrations apply dslq_research --remote",
    "supabase:prepare": "node scripts/prepare-supabase-import.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
